#!/bin/bash
# Este script hace algo útil

# Variables
USERsv1="usuario"
PASSsv1="usuario"
PASSsv2="melon"

HOST_IP="192.168.239.231"

# Actualización de nombre de maquina.
sudo -S <<< $PASSsv1 hostnamectl set-hostname Server

# Actualización de repositorios
sudo -S <<< $PASSsv1 apt update -y && sudo apt upgrade -y
# Instalar sshpass si no está instalado
sudo -S <<< $PASSsv1 apt install sshpass -y

# Conectarse a la Máquina Server directamente sin necesidad de confirmar la fingerprint
sshpass -p "$PASSsv1" ssh -o StrictHostKeyChecking=no "$USERsv1@$HOST_IP" << EOF
        echo "$PASSsv1" | sudo -S bash -c 'echo "root:$PASSsv2" | chpasswd'
EOF


sudo -S <<< usuario hostnamectl set-hostname Server
echo bash
echo "PASSsv1" | sudo -S chpasswd <<< "root:$PASSsv2"
sudo cp /etc/netplan/50-cloud-init.yaml /etc/netplan/50-cloud-init.yaml.bkup 
echo "PASSsv1" | sudo -S rm /etc/netplan/50-cloud-init.yaml
echo "PASSsv1" | sudo -S touch /etc/netplan/50-cloud-init.yaml
echo "PASSsv1" | sudo -S bash -c 'cat > /etc/netplan/50-cloud-init.yaml <<EOF
network:
    ethernets:
        enp1s0:
            dhcp4: false
            addresses:
            - $HOST_IP/22
            routes:
                - to: default
                  via: 192.168.236.1
            nameservers:
                addresses: [192.168.236.1]
    version: 2
EOF
netplan apply'
